# Definir el puerto del servidor
server.port=80

# Definir el formato de la fecha en el registro (logging)
logging.pattern.dateformat=hh:mm

# Desactivar el banner de Spring
spring.main.banner-mode=off

# Deshabilitar el caché de Thymeleaf (solo para desarrollo)
spring.thymeleaf.cache=false

# Configurar la conexión a MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/plushieshop?useTimezone=true&serverTimezone=UTC
spring.datasource.username=grupo5
spring.datasource.password=clave_bd
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Mostrar información detallada en el registro (logging)
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.type.descriptor.sql.BasicBinder=TRACE

# Definir la información para utilizar cuenta de Correo para usuarios
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mel.est0418@gmail.com
spring.mail.password=ruqn hhgx csvi koet
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Definir el servidor para enviar el correo
servidor.http=localhost
